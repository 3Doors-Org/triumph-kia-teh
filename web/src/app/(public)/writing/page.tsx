import { PageSectionSkeleton } from "@/components/layout/page-section-skeleton";

export default function WritingPage() {
  return (
    <PageSectionSkeleton
      title="Writing"
      description="Writing index shell with structural zones for categories, highlights, and paginated entries."
    />
  );
}
