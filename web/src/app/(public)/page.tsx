import { PageSectionSkeleton } from "@/components/layout/page-section-skeleton";

export default function HomePage() {
  return (
    <PageSectionSkeleton
      title="Home"
      description="Institutional landing shell with secure, tokenized structure ready for content and data integration."
    />
  );
}
