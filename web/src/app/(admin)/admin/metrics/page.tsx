import { AdminPageSkeleton } from "@/components/layout/admin-page-skeleton";

export default function MetricsAdminPage() {
  return (
    <AdminPageSkeleton
      title="Metrics"
      description="Metrics admin shell for KPI curation and future analytics integration."
    />
  );
}
