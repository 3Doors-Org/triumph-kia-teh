import { AdminPageSkeleton } from "@/components/layout/admin-page-skeleton";

export default function ExitIntentAdminPage() {
  return (
    <AdminPageSkeleton
      title="Exit Intent"
      description="Exit-intent admin shell for configuration governance and safe rollout."
    />
  );
}
