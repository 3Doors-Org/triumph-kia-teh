import { AdminPageSkeleton } from "@/components/layout/admin-page-skeleton";

export default function AchievementsAdminPage() {
  return (
    <AdminPageSkeleton
      title="Achievements"
      description="Achievements admin shell for verified entries and evidence-backed publication."
    />
  );
}
