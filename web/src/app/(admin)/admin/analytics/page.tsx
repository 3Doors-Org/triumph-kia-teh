import { AdminPageSkeleton } from "@/components/layout/admin-page-skeleton";

export default function AnalyticsAdminPage() {
  return (
    <AdminPageSkeleton
      title="Analytics"
      description="Analytics admin shell for dashboard wiring and reporting pipelines."
    />
  );
}
